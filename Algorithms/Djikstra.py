from DataStructures.HashTable import HashTable

class Djikstra():
    def __init__(self):
        self

    def dijkstra(self, source_node, destination_node): #data_structure is graph
        ht = HashTable(10)


        # sll_next_to_visit = SinglyLinkedList()



        # distance = []
        # distance[source_node] = 0
        #
        # min_priority_queue = PriorityQueue()
        #
        # for vertex in Graph:
        #
        #     if(vertex != source_node):
        #         distance[vertex] = float(infinity)
        #     prev
        #
